const validateForm = (values, validators) => {
  const err = {};
  /* console.log(values);
  validators.forEach(validator => {
    let nameValue = (""+validator.name)
    console.log(validator);
    values
  }) */

  /* if(values.username.length === 0) err.username = "Length of username is too short";

  return err; */
}

export default validateForm;